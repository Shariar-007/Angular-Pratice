Greeting Counter

Project 1 of a hands-on series for learning Angular from the ground up.

The goal of this project is not just to build a working app. it's to create every
piece (component, module, service, template, style, directive, TypeScript model) by
hand and wire them together manually, so the connections Angular normally hides behind
the CLI become clear.


